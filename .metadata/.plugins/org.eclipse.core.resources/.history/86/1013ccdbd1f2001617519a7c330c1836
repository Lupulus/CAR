package Command;

import Request.FtpRequest;

public class CommandPasv extends Command {
	
	public CommandPasv(FtpRequest ftp){
		super(ftp);
		ftp.getRequest().setServerSocket(new ServerSocket(0));
	}
}
