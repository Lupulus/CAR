package Command;

import java.io.File;

import Request.FtpRequest;

public class CommandPwd extends Command{
	public CommandPwd(FtpRequest ftp, File repertoire) {
		super(ftp);
		process(repertoire);
	}
	
	public void process(File repertoire){
		String[] temp = repertoire.toString().split("/servorFile");
		System.out.println(temp[1] + "    " + temp[2]);
		//ftp.send(257, repertoire.toString().substring(repertoire.toString().lastIndexOf("/")));
	}
}
